#This file will be used if we want to customize the Ruff configuration.

[lint]
# The codebase follows one-import-per-line (see the Python style guide), which
# ruff's isort rule (I001) would otherwise rewrite into grouped imports.
ignore = ["I001"]
